<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Device Admin for Lockdown -->
    <uses-permission android:name="android.permission.BIND_DEVICE_ADMIN" />

    <!-- Camera for Written Test Photos -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Storage -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Alarm -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Foreground Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.GetYourSuccess"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Notes Activity -->
        <activity
            android:name=".activities.NotesActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Test Activity -->
        <activity
            android:name=".activities.TestActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Written Test Activity -->
        <activity
            android:name=".activities.WrittenTestActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Study Planner Activity -->
        <activity
            android:name=".activities.StudyPlannerActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Performance Dashboard -->
        <activity
            android:name=".activities.PerformanceDashboardActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Device Admin Receiver for Lockdown -->
        <receiver
            android:name=".services.DeviceLockReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Alarm Service -->
        <service
            android:name=".services.AlarmService"
            android:enabled="true"
            android:exported="false" />

        <!-- Foreground Service for Lockdown -->
        <service
            android:name=".services.LockdownService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="manifest" />

        <!-- Notification Receiver -->
        <receiver
            android:name=".receivers.NotificationReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Firebase Messaging Service -->
        <service
            android:name=".services.FirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
